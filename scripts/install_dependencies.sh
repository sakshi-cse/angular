#!/bin/bash

# Change to the project directory
cd /var/www/html

# Install Node.js dependencies using npm or yarn
# Replace the package manager command with the appropriate one for your project

# If using npm:
npm install

# If using yarn:
# yarn install

# Build the Angular project
ng build --prod
